El servidor si no le ponemos nada en la URL nos devuelve el archivo usuario.html, si queremos acceder al archivo sensores.html tendremos que ponerlo en la URL manualmente. 

Los valores introducidos en el formulario deben ser números enteros.
