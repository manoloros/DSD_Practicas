Funcionamiento:

Los parámetros que hay que darle al ejecutar calculadora_client son:

./calculadora_client localhost <un_entero> <operador> <otro_entero>

Aplicará la operación de ese operador (Operador puede ser: +,-,x,/) a esos dos enteros.


